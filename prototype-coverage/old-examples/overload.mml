(* This won't work until you implement subtyping *)
let
  fun square : (int -> int) & (real -> real) (x) =
    x * x

  val intsquare = square 9
  val floatsquare = square 0.5
in
  (intsquare, floatsquare)
end;
