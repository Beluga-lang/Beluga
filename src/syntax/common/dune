(library
 (name common) ;; TODO: Rename to [syncom]
 (package beluga)
 (libraries support sedlex)
 (instrumentation
  (backend bisect_ppx --bisect-silent yes))
 (preprocess
  (per_module
   ((pps sedlex.ppx)
    error))))
