module Nats = struct
  nat : type.
  z : nat.
  s : nat -> nat.

  private rec suc : [ |- nat ] -> [ |- nat] =
  fn n => let [ |- N ] = n in [ |- s N];

  private let two = [ |- s (s z)];
  private let three = suc two;
end;

let x = [ |- Nats.s Nats.z];

let will_fail = Nats.two;